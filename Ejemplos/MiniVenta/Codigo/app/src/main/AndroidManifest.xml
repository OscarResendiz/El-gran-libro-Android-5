<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="oscar.com.miniventa">

    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".CerrarVentaActivity"></activity>
        <activity android:name=".ActivitySeleccionarArticulo" />
        <activity android:name=".ArticuloVenta" />
        <activity
            android:name=".Loguin"
            android:theme="@style/Theme.AppCompat.Dialog">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Principal" />
        <activity
            android:name=".PantallaRegistro"
            android:label="Alta" />
        <activity
            android:name=".PantallaCatalogo"
            android:label="Catalogo" />
        <activity
            android:name=".PropiedadesCliente"
            android:label="@string/Cliente" />
        <activity
            android:name=".PropiedadesGrupo"
            android:label="@string/Grupo" />
        <activity
            android:name=".PropiedadesArticulo"
            android:label="@string/Articulo" />
        <activity
            android:name=".PropiedadesMovimiento"
            android:label="@string/Movimientos" />
        <activity
            android:name=".PantallaVenta"
            android:label="@string/Venta" />
        <activity
            android:name=".MessageBox"
            android:label="ELIMINAR"
            android:theme="@style/Theme.AppCompat.Dialog" />
        <activity
            android:name=".SeleccionarGrupoActivity"
            android:label="Seleccione el grupo"
            android:theme="@style/Theme.AppCompat.Dialog" />
        <activity android:name=".SeleccionarClienteActivity" />
        <activity android:name=".PrpiedadesArticuloVentaActivity" />
    </application>

</manifest>